export const SNEAKER_MODEL_PATH = "/models/sneaker.glb";

/** Display scale applied after GLTF load (Khronos shoe units) */
export const SNEAKER_DISPLAY_SCALE = 2.65;

/** Root offset — sole sits on shadow plane */
export const SNEAKER_ROOT_OFFSET: [number, number, number] = [
  0.08, -0.92, 0.02,
];
